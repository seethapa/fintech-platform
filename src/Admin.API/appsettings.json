{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  // 🔐 Azure AD (NOT secret)
  "JwtIssuer": "https://login.microsoftonline.com/<TENANT_ID>/v2.0",
  "JwtAudience": "api://<API-APP-CLIENT-ID>",

  // 🔐 Key Vault (NOT secret)
  "KeyVaultUrl": "https://<your-keyvault-name>.vault.azure.net/"
}